SCOS Field Migration (retired)
------------------------------
Files: archive/scos-migration.php (full script, reference only)
       scos-migration.php at repo root (deprecated stub — deploys with MU package)

All client sites are migrated. The Tools → SCOS Migration screen is retired:
- site-essentials/Core/Migration_Deprecated.php removes the menu and blocks POSTs
- Root scos-migration.php is a no-op stub (replaces the old standalone mu-plugin)

Emergency re-run only: copy archive/scos-migration.php and define
SCOS_MIGRATION_FORCE_LEGACY true before it loads (not supported in production).

Remove scos-migration.php from mu-plugins on each server when convenient.
